openapi: 3.0.0
info:
  title: API Banca en Línea
  description: Documentación de la API para el proyecto de Banca en Línea
  version: 1.0.0
servers:
  - url: http://localhost:3000
    description: Servidor Local
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
paths:
  /:
    get:
      summary: Ruta base de prueba
      tags: [General]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: Servidor de Banca Online Operativo
  /api/auth/register:
    post:
      summary: Registrar un nuevo usuario
      tags: [Auth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - email
                - password
                - nombre
              properties:
                email:
                  type: string
                  format: email
                password:
                  type: string
                  format: password
                nombre:
                  type: string
      responses:
        '201':
          description: Usuario registrado exitosamente
        '400':
          description: Faltan campos requeridos o error de registro
        '500':
          description: Error interno del servidor
  /api/auth/login:
    post:
      summary: Iniciar sesión
      tags: [Auth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - email
                - password
              properties:
                email:
                  type: string
                  format: email
                password:
                  type: string
                  format: password
      responses:
        '200':
          description: Login exitoso, devuelve el token JWT
        '400':
          description: Faltan campos requeridos
        '401':
          description: Credenciales inválidas
        '500':
          description: Error interno del servidor
  /api/transacciones/abonar:
    post:
      summary: Realizar un abono a una cuenta
      tags: [Transacciones]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - usuario_id
                - monto
              properties:
                usuario_id:
                  type: string
                  format: uuid
                  description: ID del usuario al que se le abonará el monto
                monto:
                  type: number
                  format: float
                  description: Monto a abonar
      responses:
        '200':
          description: Abono realizado con éxito
        '400':
          description: Faltan datos o el monto es inválido
        '401':
          description: No autorizado (Token inválido o faltante)
        '404':
          description: Usuario no encontrado
        '500':
          description: Error interno del servidor
  /api/transacciones/transferir:
    post:
      summary: Realizar una transferencia entre cuentas
      tags: [Transacciones]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - usuario_id_origen
                - usuario_id_destino
                - monto
              properties:
                usuario_id_origen:
                  type: string
                  format: uuid
                  description: ID del usuario que envía el dinero
                usuario_id_destino:
                  type: string
                  format: uuid
                  description: ID del usuario que recibe el dinero
                monto:
                  type: number
                  format: float
                  description: Monto a transferir
      responses:
        '200':
          description: Transferencia realizada con éxito
        '400':
          description: Faltan datos, monto inválido o saldo insuficiente
        '401':
          description: No autorizado (Token inválido o faltante)
        '404':
          description: Usuario origen o destino no encontrado
        '500':
          description: Error interno del servidor
